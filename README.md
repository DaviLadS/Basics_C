## Basics_C

Aqui fiz meu primeiro código em Linguagem C, onde uso o comando de saída (printf) para mostrar uma mensagem na tela do usuário que executar o código, com comandos auxiliares para fazer o código ficar na tela (system("pause");).
